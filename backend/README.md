# Backend

Node.js + Express backend for the AI Coding Assistant.

## Run

npm install
npm run dev

## Environment

Copy `.env.example` to `.env` and provide:
- `GEMINI_API_KEY`
- `MODEL`
- `TARGET_PROJECT_ROOT`
- `PORT`
